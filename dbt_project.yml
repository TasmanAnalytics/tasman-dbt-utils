name: 'tasman_dbt_utils'
version: '0.0.1'
config-version: 2

require-dbt-version: [">=1.3.0", "<2.0.0"]

macro-paths: ["macros"]
model-paths: ["models"]
test-paths: ["tests"]
log-path: "logs"

target-path: "target"
clean-targets:
  - "target"
  - "dbt_modules"
  - "dbt_packages"
